# Existing server variables remain unchanged.
# Add the deployed scanner app origin to CORS, for example:
# SCANNER_ORIGIN=https://your-scanner-domain.example

# Separate live attendance Vercel origin
LIVE_ATTENDANCE_ORIGIN=https://your-live-attendance.vercel.app
